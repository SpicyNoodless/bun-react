import { observer } from "mobx-react-lite"

export const MetadataCompareView = observer(() => {
  return (
    <div>
      <h1>Metadata Component</h1>
      <p>This component is currently under development.</p>
    </div>
  )
})
